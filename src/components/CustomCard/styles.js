import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  root: {
    width: 350,
    height: 'auto',
    marginBottom: 20,
  },
 
  media: {
    backgroundSize: 'contain',
    height: 'auto',
    paddingTop: '60%', 
  },
  cardHeader: {
    textAlign: 'center'
  }
  
}));
